#name : CI

#on:
# push:
#  branches : [main]

#jobs:
# test:
#  runs-on:ubuntu-latest
# steps:
#  - uses: actions/checkout@v4
#  - name: Run a command
#   run : echo "hello from linux  this is first github action"




name: CI

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run a command
        run: echo "hello from linux this is first github action"
